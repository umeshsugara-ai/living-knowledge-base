# secrets
**/.env
**/.env.*
!**/.env.example

# node
**/node_modules/
**/dist/
**/*.tsbuildinfo

# playwright artifacts
**/.playwright-mcp/

# large media (raw data lives under raw/, never in the code tree — D-003)
raw/TOC/TOC-Materials/Audio/
raw/TOC/TOC-Materials/Recordings/

# sources/whatsapp_msg is a git submodule with its own .gitignore (media/, sessions/, .env)

# reference screenshots (large, regenerable)
reference/kb-deck-screens/*.jpg
reference/kb-deck-screens/*.png

# logs
*.log

# python bytecode
**/__pycache__/
*.pyc

# claude-code local state
.claude/scheduled_tasks.lock
.handoffs/

# goal tracker regenerated artefacts
.goal/dashboard.html
.goal/monitor_task.cmd
.goal/monitor_task.vbs
.goal/audits/

# live-verify runs: the reconcilable facts (preflight.json + summary.md) ARE committed; the
# bulk (page screenshots, raw response bodies) is regenerable by re-running
# `LKB_API_KEY=... pnpm verify:live`, which is the whole point of the protocol -- committing
# ~2MB of binaries per run would bloat the repo for evidence anyone can reproduce on demand.
qa/evidence/live-*/pages/
qa/evidence/live-*/api/

apps/api/src/routes/ghost.ts
