2026-09-10T07:54:11+05:30 · HUMAN_GATE · T-007 first implementation unit mapped: extend the existing claims schema/generated type/fixtures into a WhatsApp candidate lifecycle, then change indexSession plus its existing fake/tests to use tenant-namespaced stable candidate ids and preserve approved/rejected decisions across re-index. Filtering Ask alone is rejected because current indexSession also materialises session_pages, chunks, tree_index and promoted entities before review. Project anti-drift rules require Umesh to approve this exact multi-file plan before edits; no new product module is proposed. Real Mongo remains unavailable, GitHub export remains gated, and user-owned .goal/runtime projection files remain untouched.
