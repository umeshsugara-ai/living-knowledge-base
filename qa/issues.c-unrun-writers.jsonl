{"id": "ISS-C-UNRUN-WRITERS-001", "date": "2026-09-08", "severity": "medium", "feature": "watched-sources-entrypoint", "title": "Route validates a parsed URL but stores the raw string — validated value != stored value", "evidence": "apps/api/src/routes/watched-sources.ts:31-39 parses via new URL(value); line 70 stores `url: body.url` verbatim. Probe: a value with leading/trailing spaces (\" https://ok.com \") and one with a trailing newline are both accepted and stored with the whitespace intact; a value written with backslashes after the scheme (http: slash backslash slash backslash evil.com) is accepted, because WHATWG normalises backslashes to slashes giving host evil.com, and is then stored with the backslashes still in it. A future fetcher using a different URL parser (curl, python-requests, Go net/url) may resolve or reject the stored string differently from the one this route approved.", "status": "fixed", "found_by": "checker-unit", "fixed_date": "2026-09-08", "verified_date": null, "checker_note": "Verified closed at cycle 1, commit c8da5ee: normalisedHttpUrl() returns parsed.href and the route stores that value (watched-sources.ts:38-46,74). Re-probed - \" https://ok.com \n\" stores https://ok.com/ and the backslash form http:/\\/\\evil.com/ stores http://evil.com/, so the raw string no longer reaches the collection. Admission predicate byte-for-byte unchanged, so no new bypass. Stays fixed (not verified) until a later re-check."}
{"id": "ISS-C-UNRUN-WRITERS-002", "date": "2026-09-08", "severity": "high", "feature": "watched-sources-entrypoint", "title": "No internal-address block anywhere in the Watched Sources pipeline — stored SSRF, owed by the fetcher unit", "evidence": "Probe against the route's isHttpUrl: http://169.254.169.254/latest/meta-data/, http://localhost:27017/, http://127.0.0.1:6379/, http://[::1]:8080/ and http://10.0.0.5/ all pass and are stored. NOT a defect of this unit — a store-time hostname check cannot survive DNS rebinding between registration and fetch, so the sound place is the fetcher, on the resolved IP after DNS. Filed against the unbuilt fetcher/scheduler unit and recorded as contract invariant [I3] so it cannot fall between two units neither of which owns it.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
{"id": "ISS-C-UNRUN-WRITERS-003", "date": "2026-09-08", "severity": "low", "feature": "watched-sources-entrypoint", "title": "URLs with embedded credentials are accepted and stored in plaintext", "evidence": "Probe: https://user:pass@evil.com/ passes isHttpUrl (apps/api/src/routes/watched-sources.ts:31) and is stored verbatim. GET /watched-sources then returns the credentials to any key with the sources scope, and a future fetcher will likely log them.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
{"id": "ISS-C-UNRUN-WRITERS-004", "date": "2026-09-08", "severity": "low", "feature": "watched-sources-entrypoint", "title": "No de-duplication: registering the same URL twice creates two rows", "evidence": "Disclosed by the maker as gap 3; confirmed — apps/api/src/routes/watched-sources.ts:69-76 always inserts, and createWatchedSource (packages/db/src/collections/watched-sources.ts:13) is an unconditional insertOne. Two rows means the future scheduler fetches the same URL twice per interval. Out of scope for this unit per the contract; filed so it is not lost.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
{"id": "ISS-C-UNRUN-WRITERS-005", "severity": "high", "status": "open", "found_by": "maker", "date": "2026-09-08", "title": "mc-precommit.ps1 resolves the armed-mutation ledger against CLAUDE_PROJECT_DIR, not the worktree being committed to, so ONE lane's live mutation blocks commits in EVERY other lane", "evidence": "Observed 2026-09-08 in D:\\KnowledgeBase-lanes\\c-unrun-writers. Committing the guarded-fetcher unit was refused with 'BLOCKED: a mutation is still armed ... apps/api/src/indexing/vector-gap.ts'. That file does not exist in this worktree at all -- it is the OTHER maker loop's in-flight work in the main tree. Cause: .claude/hooks/mc-precommit.ps1:13 does `if ($env:CLAUDE_PROJECT_DIR) { Set-Location $env:CLAUDE_PROJECT_DIR }` and then reads `qa/.mutations-active` at :21, so every worktree's pre-commit consults D:\\KnowledgeBase's ledger. The main tree's ledger currently holds {\"path\":\"apps/api/src/indexing/vector-gap.ts\",\"pid\":38932}. The guard's INTENT is right and was earned the hard way (it exists because the ISS-083 score:0.5 mutation was found applied to production source), but its scope is the repo rather than the checkout.", "impact": "A lane cannot commit while any other lane is mid-mutation-test, which is most of the time under the concurrent-loop model. Worse for safety than for throughput: the natural workaround is to delete another lane's ledger row or bypass the hook, which disarms a live control protecting production source. I did neither -- the commit is being held instead.", "fix_direction": "Resolve the ledger against the worktree being committed to: `git rev-parse --show-toplevel` (or $PWD before the Set-Location) rather than CLAUDE_PROJECT_DIR, so each checkout has its own qa/.mutations-active. Keep the CLAUDE_PROJECT_DIR fallback for the non-worktree case. ENFORCEMENT PATH: .claude/hooks/* changes require an authorizing DECISIONS entry carrying 'Approved-by: Umesh' per the project CLAUDE.md, so this is NOT a maker fix -- it needs the Approver. Fourth cross-loop leak today after ISS-100, ISS-102/103 and the divergent-id gate; see qa/gates/ledger-id-divergence.md and qa/gates/concurrent-maker-sessions.md."}
{"id": "ISS-C-UNRUN-WRITERS-006", "date": "2026-09-08", "severity": "high", "feature": "guarded-fetcher", "title": "Range table blocks the dotted spelling of v4-embedding IPv6 prefixes but allows the hex spelling — 6to4, NAT64 and Teredo walk through", "evidence": "packages/ingest/src/sources/guarded-fetch.ts:74-86. The unwrap at :77-79 reads only the text after the LAST colon, so it fires only when the embedded v4 is written in dotted form. Probed against the shipped isBlockedAddress: '64:ff9b::127.0.0.1' -> BLOCK but '64:ff9b::7f00:1' (same address, NAT64, embeds 127.0.0.1) -> ALLOW; '2002:7f00:0001::1' (6to4, embeds 127.0.0.1) -> ALLOW; '2002:c0a8:0101::1' (6to4, embeds 192.168.1.1) -> ALLOW. 2001::/32 (Teredo) is likewise unlisted. An attacker controls their own AAAA record, so emitting any of these is free, and on a host with 6to4 or a NAT64/DNS64 network these route to the embedded v4 — precisely the ranges blockedIpv4 exists to refuse. The table's own dotted-form block shows the intent; the hex form is the gap.", "fix_direction": "Parse the v6 address into its 8 groups rather than string-matching a tail. Block 2002::/16, 64:ff9b::/96 and 2001::/32 outright, and for ::ffff:0:0/96, ::/96, 2002::/16 and 64:ff9b::/96 reconstruct the embedded v4 from the group values and run blockedIpv4 on it, so both spellings reach the same verdict. Add a test per prefix in BOTH notations.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
{"id": "ISS-C-UNRUN-WRITERS-007", "date": "2026-09-08", "severity": "high", "feature": "guarded-fetcher", "title": "Module docstring claims the checked resolution is the one the caller uses; nothing pins it, so the check-to-connect rebinding window is fully open", "evidence": "packages/ingest/src/sources/guarded-fetch.ts:16-17 states 'lookup and request are injected so ... the resolution the guard checks is the same one the caller will use', and the commit message for 43c1281 repeats it. But the injected seam is `request(url: string)` (:27) and the call site is `deps.request(current.href)` (:123) — request receives a HOSTNAME and performs its own independent resolution. The address isBlockedAddress approved at :119 is never handed to the transport and never pinned. The unit therefore closes the register-to-fetch window (its real and valuable contribution) but leaves the check-to-connect window entirely open, which is the window classic DNS rebinding actually exploits. Verified by reading the seam signature, not inferred.", "fix_direction": "Either widen the seam to `request(url: string, address: string)` and require the transport to connect to that address with the original Host header (real pinning), or amend the docstring and the next commit message to state plainly that the checked address is not pinned and name the residual window. The claim as written tells a future reader the module has a property it does not have.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
{"id": "ISS-C-UNRUN-WRITERS-008", "date": "2026-09-08", "severity": "medium", "feature": "guarded-fetcher", "title": "fec0::/10 deprecated IPv6 site-local is allowed by the link-local regex", "evidence": "packages/ingest/src/sources/guarded-fetch.ts:83 uses /^fe[89ab][0-9a-f]?:/ which matches fe80::/10 only. Probed: isBlockedAddress('fec0::1') -> ALLOW. fec0::/10 is deprecated (RFC 3879) but is still configured on legacy and appliance networks, and a deny-list guard has no reason to admit it.", "fix_direction": "Widen to /^fe[89a-f]/ or block fe00::/8 wholesale — nothing in fe00::/9 is a legitimate public fetch target either. Add a test.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
{"id": "ISS-C-UNRUN-WRITERS-009", "date": "2026-09-08", "severity": "medium", "feature": "guarded-fetcher", "title": "maxBytes is asserted after the whole body is already in memory, is skipped entirely on redirect hops, and there is no request timeout at all", "evidence": "packages/ingest/src/sources/guarded-fetch.ts:132-134 checks res.body.length AFTER `request` has returned a fully materialised string, so a hostile endpoint has already been allowed to allocate it — the cap documents an intent the code does not enforce. The redirect branch at :125-130 returns before the size check, so redirect bodies are unbounded regardless. The GuardedFetchDeps interface (:22-29) has no timeout field and guardedFetch never races a deadline, so a slow-loris endpoint hangs an unattended timer-driven fetcher indefinitely. This project already has D-020 because an unbounded hang took out the test suite today.", "fix_direction": "Move the cap into the transport seam (pass maxBytes to `request` and have it abort the stream when exceeded), apply it on every hop, and add a per-request timeout to GuardedFetchDeps enforced with an AbortSignal or Promise.race.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
{"id": "ISS-C-UNRUN-WRITERS-010", "date": "2026-09-08", "severity": "high", "feature": "guarded-fetcher", "title": "Unit was dispatched for check with no evidence manifest on disk — the maker-checker handshake was bypassed", "evidence": "The dispatch names qa/manifests/guarded-fetcher.md at Status ready-for-check, Fix cycle 1. That file does not exist in the worktree (`ls qa/manifests | grep -i guard` returns only three unrelated files) and has never existed in git history (`git log --all -- qa/manifests/guarded-fetcher.md` is empty). Commit 43c1281 ships the code, the tests and a ledger line, but no manifest. The project CLAUDE.md is explicit that 'the maker-checker handshake is files, never memory: maker's request = manifest at Status: ready-for-check (with Fix cycle: N)'. The check proceeded on the committed artefact because real security defects were reachable without it, but the claimed verify commands, the disclosed gaps and the mutation table existed only in a commit message — the exact transcript-only failure mode the manifest rule exists to kill. The commit message's mutation row for the address check ('47/13') also does not sum to the 61-test suite; the reproduced figure is 48/13.", "fix_direction": "Write qa/manifests/guarded-fetcher.md retroactively for cycle 1 with the actual verify commands, the disclosed gaps and the mutation table, then flip it to Fix cycle 2 with the fixes for ISS-006 through ISS-009. No further dispatch without the manifest landing first.", "status": "open", "found_by": "checker-unit", "fixed_date": null, "verified_date": null}
