# Manifest — T-017b-snapshot-features-ledger
**Contract:** qa/contracts/snapshot-features-ledger.md
**Goal task:** T-017b
**Date:** 2026-09-03
**Fix cycle:** 2 of max 3
**Issues addressed:** ISS-013

## Fix cycle 2 — response to verdict (qa/verdicts/T-017b-snapshot-features-ledger.md, Cycle checked: 1)
- **[C6a] ISS-013 "SessionEnd hook written but not registered in settings.json"** → D-009 written
  (`docs/DECISIONS.md`, `Approved-by: Umesh`) authorizing exactly this settings.json change.
  `.claude/hooks/features-snapshot-session-end.ps1` (already built cycle 1, unchanged) is now
  registered as a second `SessionEnd` entry in `.claude/settings.json`, merged alongside the
  existing `lab-session-end.ps1` entry (not overwritten). Hand-run: `echo '{}' | ... ->` exit 0.
  `python -c "import json; json.load(open('.claude/settings.json'))"` → valid JSON.

## What changed
- `schema/features_event.schema.json` (new) — single-event schema for one `docs/FEATURES.jsonl`
  line: required `{id, feature, event, date, unit, links}`; `event ∈ {planned, shipped, updated,
  removed}`; `reason` (nullable/omittable) becomes required + non-empty via an `allOf/if/then`
  conditional whenever `event ∈ {updated, removed}`. Fits `schema/validate.py`'s existing
  per-collection single-doc fixture loop exactly like the other 10 collections — **no change to
  validate.py was needed** [C1].
- `schema/fixtures/features_event.valid.json`, `schema/fixtures/features_event.invalid.json`
  (new) — invalid fixture is a `removed` event with no `reason`, proving the conditional actually
  rejects (not vacuous).
- `scripts/lib/mini-schema.mjs` (new, 70 LOC) — small dependency-free interpreter for the JSON
  Schema Draft 2020-12 subset this repo uses (type/enum/pattern/minLength/required/properties/
  items/allOf-if-then). No JS schema-validator package (ajv etc.) is in `package.json` yet; this
  reads `schema/features_event.schema.json` itself at runtime rather than hand-copying its field
  list, so the schema file stays the one definition of shape (ARCHITECTURE §5).
- `scripts/lib/features.mjs` (new, 40 LOC) — `appendEvent(entry, opts)`: validates against the
  schema, then `fs.appendFileSync`s one JSON line to `docs/FEATURES.jsonl` (never rewrites prior
  lines); throws with the schema errors (including the missing-`reason` case) on an invalid
  entry, so nothing is written on failure. `readEvents(path)` helper reused by the snapshot.
- `scripts/features-ledger.mjs` (new, 32 LOC) — CLI (`node scripts/features-ledger.mjs add
  <json-file>`) + re-exports `appendEvent` for programmatic/test use [C2].
- `scripts/lib/snapshot-lib.mjs` (new, 140 LOC) — pure functions: `extractSection` (verbatim
  `## N.` section text), `directoryTree` (depth-2, gitignore-aware, per-top-dir purpose from
  `package.json` `description` else the ARCHITECTURE §4 `| Path | Owns |` table), `schemaSummaryTable`
  (collection / required fields / has-evidence-field), `featuresTail` (last N ledger rows as a
  table), `buildSnapshot` (assembles all of it under the `DO NOT EDIT` generated header) [C3].
- `scripts/snapshot.mjs` (new, 73 LOC) — `node scripts/snapshot.mjs` writes `docs/SNAPSHOT.md`;
  `--check` regenerates in memory and does a line-by-line diff against the committed file,
  printing which lines diverged (not just pass/fail) before checking the 200-line budget [C3b, C4].
- `package.json` — `lint:structure` chain extended with `node scripts/snapshot.mjs --check`
  (same script, one more `&&` step — no second lint entry point) [C4, C6b]; `test:lint` extended
  to also run `scripts/snapshot.test.mjs` [C8].
- `docs/FEATURES.jsonl` (new) — seeded via `appendEvent` (not hand-written) with 5 `shipped` rows
  for T-001/T-004/T-005/T-016/T-017, `unit` = the primary artifact path and `date`/reason context
  pulled from the already-committed manifests/verdicts (all dated 2026-09-03; `reason: null` since
  these are `shipped`, not `updated`/`removed`, so no reason is required or invented) [C9].
- `docs/SNAPSHOT.md` (new, generated, 93 lines) — committed output of `node scripts/snapshot.mjs`
  so `--check` has something to check against [C3, C4].
- `.claude/hooks/mc-sessionstart.ps1` — extended (edited in place, the file D-006 already grants
  the maker-checker skill write access to) with a block that reads `docs/FEATURES.jsonl` and
  prints `FEATURE CHANGED: <feature> <event> on <date> -- <reason>` for every `removed`/`updated`
  row from the last 30 days — the feature-level anti-cyclic guard mirroring the decision-level
  DECISION INDEX [C5].
- `packages/core/src/generated/features_event.ts`, `packages/core/src/index.ts` — regenerated by
  `pnpm gen:types` (untouched by hand) so the new schema doesn't leave `gen:types --check` failing.
- `scripts/snapshot.test.mjs` (new, 82 LOC, `node --test`) — the 4 cases required by C8.

## NOT done — C6a is only partially met, and here is exactly why
- `.claude/hooks/features-snapshot-session-end.ps1` (new file, staged) — a sibling SessionEnd
  hook that regenerates `docs/SNAPSHOT.md` via `node scripts/snapshot.mjs` when
  `git status --porcelain -- docs/FEATURES.jsonl` shows a change this session. **Hand-tested
  standalone** (see below) and it works.
- It is **NOT wired into `.claude/settings.json`**. `.claude/hooks/decisions-append-guard.ps1`
  (F1/"guard the guards") fires an ASK on any Write/Edit to `.claude/settings.json` and requires
  an authorizing `docs/DECISIONS.md` entry carrying `Approved-by:` from the repo's named Approver
  (Umesh) *written first* via `scripts/append_decision.ps1` (its own V7 check enforces the same
  rule). `docs/DECISIONS.md` has no existing entry covering this SessionEnd hook (grepped for
  "snapshot", "FEATURES.jsonl", "T-017b", "SessionEnd" — no hits; D-006's
  `Changes-authorized:` is scoped narrowly to "mc session-start + commit guard", not this hook).
  I made an initial edit to `.claude/settings.json` to register it, caught that it wasn't covered
  by any authorizing decision, and **reverted it** (`git diff .claude/settings.json` now empty)
  rather than bypass the gate. This is a genuine "decision only Umesh can make" per the global
  execution-discipline rule — registering a new enforcement hook needs his `Approved-by:` in a
  DECISIONS entry before it can be wired, not a judgment call I can default through.
  **Next step for whoever picks this up:** get Umesh's approval, append a D-NNN entry via
  `scripts/append_decision.ps1` with `Changes-authorized: .claude/settings.json (register
  features-snapshot-session-end.ps1 as a second SessionEnd hook)`, then add the hook entry
  (pattern: copy the `lab-session-end.ps1` entry in `.claude/settings.json`, same
  `-InputJson $i` piping, pointed at `features-snapshot-session-end.ps1`).
- Criterion 6(b) — `pnpm lint:structure` failing when `docs/SNAPSHOT.md` is stale — **is fully
  met** regardless (see `snapshot.mjs --check` wired into `lint:structure` above), so the
  contract's own fallback ("no separate `/landplane` dependency required for T-017b to pass")
  still holds: staleness is caught even with 6(a) only half-wired.

## How to verify (exact commands, run from D:\KnowledgeBase)

**C1 — schema + validate.py:**
```
python schema/validate.py
```
Expect `OK: features_event — valid fixture passes, invalid fixture correctly rejected (1
error(s))` among the 11 lines, and `PASS: 11 collection schema(s) validated correctly.`

**C2 — appendEvent / CLI:**
```
node --test scripts/snapshot.test.mjs
```
(covers reject-on-missing-reason and append-only directly; see C8 below for the same run)

**C3 / C3b / C4 — snapshot generation, drift-catching, budget:**
```
node scripts/snapshot.mjs --check
wc -l docs/SNAPSHOT.md
```

**C5 — session-start hook, hand-run proof (simulated stdin, real output captured this run):**
```
$ cp docs/FEATURES.jsonl docs/FEATURES.jsonl.bak
$ node --input-type=module -e "import { appendEvent } from './scripts/lib/features.mjs'; appendEvent({id:'T-999', feature:'test-removed-feature', event:'removed', date:'2026-08-20', unit:'scripts/test.mjs', reason:'superseded by T-1000 (test row for hook proof)', links:['qa/manifests/T-999-fake.md']});"
$ powershell -NoProfile -ExecutionPolicy Bypass -Command "& '.claude/hooks/mc-sessionstart.ps1'" | grep -i "FEATURE CHANGED"
FEATURE CHANGED: test-removed-feature removed on 2026-08-20 -- superseded by T-1000 (test row for hook proof)
$ mv docs/FEATURES.jsonl.bak docs/FEATURES.jsonl   # restore the seeded 5-row ledger
$ node scripts/snapshot.mjs                         # regenerate SNAPSHOT.md to match
```
(the test row was never left in the committed ledger — restored before staging)

**C6b — lint:structure gate:**
```
pnpm lint:structure
```

**C7 — memory pointer:** file contents pasted below (not git-tracked; lives outside the repo).

**C8 — tests:**
```
node --test scripts/snapshot.test.mjs
pnpm test:lint
```

**C9 — seed data:**
```
cat docs/FEATURES.jsonl
```

**Full CI-equivalent sequence:**
```
pnpm install
pnpm -r typecheck && pnpm -r test
pnpm gen:types --check
python schema/validate.py
pnpm lint:structure
pnpm test:lint
```

## Actual outputs (this run, verbatim)

### `python schema/validate.py`
```
OK: claims — valid fixture passes, invalid fixture correctly rejected (1 error(s))
OK: decisions — valid fixture passes, invalid fixture correctly rejected (1 error(s))
OK: features_event — valid fixture passes, invalid fixture correctly rejected (1 error(s))
OK: orgs — valid fixture passes, invalid fixture correctly rejected (1 error(s))
OK: session_pages — valid fixture passes, invalid fixture correctly rejected (1 error(s))
OK: sessions — valid fixture passes, invalid fixture correctly rejected (3 error(s))
OK: sources — valid fixture passes, invalid fixture correctly rejected (4 error(s))
OK: speakers — valid fixture passes, invalid fixture correctly rejected (2 error(s))
OK: topics — valid fixture passes, invalid fixture correctly rejected (1 error(s))
OK: tree_index — valid fixture passes, invalid fixture correctly rejected (2 error(s))
OK: turns — valid fixture passes, invalid fixture correctly rejected (3 error(s))

PASS: 11 collection schema(s) validated correctly.
```

### `node scripts/snapshot.mjs --check` (fresh) then hand-edit then `--check` again
```
OK: docs/SNAPSHOT.md matches a fresh regeneration (93 lines, budget 200)
--- after appending "tampered" to the file ---
FAIL: docs/SNAPSHOT.md is stale (2 line(s) differ from a fresh regeneration):
  line 94: committed="tampered" fresh=""
  line 95: committed="" fresh=""
--- regenerated back to clean, re-checked ---
OK: docs/SNAPSHOT.md matches a fresh regeneration (93 lines, budget 200)
```

### `wc -l docs/SNAPSHOT.md` → 93 (budget 200)

### `node --test scripts/snapshot.test.mjs`
```
✔ appendEvent: rejects a removed event with no reason (2.3168ms)
✔ appendEvent: is append-only (3.0858ms)
✔ snapshot.mjs --check: 0 fresh, non-zero after a hand-edit (222.0565ms)
✔ snapshot.mjs: current repo's docs/SNAPSHOT.md is <= 200 lines (63.3255ms)
ℹ tests 4
ℹ pass 4
ℹ fail 0
```

### `pnpm test:lint`
```
✔ lint-loc: passes on a clean fixture
✔ lint-loc: fails on a violating fixture
✔ lint-dirsize: passes on a clean fixture
✔ lint-dirsize: fails on a violating fixture
✔ lint-root: passes on a clean fixture
✔ lint-root: fails on a violating fixture
✔ lint-dupes: passes on a clean fixture
✔ lint-dupes: fails on a violating fixture
✔ lint-migrations: passes on a clean fixture
✔ lint-migrations: fails on a violating fixture
✔ appendEvent: rejects a removed event with no reason
✔ appendEvent: is append-only
✔ snapshot.mjs --check: 0 fresh, non-zero after a hand-edit
✔ snapshot.mjs: current repo's docs/SNAPSHOT.md is <= 200 lines
ℹ tests 14
ℹ pass 14
ℹ fail 0
```

### `pnpm lint:structure`
```
lint-loc: OK (30 file(s) within budget)
lint-dirsize: OK (26 dir(s) within budget)
lint-root: OK (12 loose root file(s), 1 gitignored excluded)
lint-dupes: OK (17 unique export(s), 11 unique schema $id(s))
lint-migrations: OK (396 file(s) scanned)
OK: docs/SNAPSHOT.md matches a fresh regeneration (93 lines, budget 200)

✔ no dependency violations found (28 modules, 33 dependencies cruised)
```

### `pnpm gen:types -- --check`
```
OK: 11 generated type file(s) + index.ts match schema/
```

### `pnpm -r typecheck` and `pnpm -r test`
All 9 workspace projects typecheck clean; `packages/index` (4 tests) and `packages/ask` (6
tests) both pass 0 failures (the two packages with `test` scripts as of T-016).

### `cat docs/FEATURES.jsonl`
```
{"id":"T-001","feature":"knowledge-base-schema","event":"shipped","date":"2026-09-03","unit":"schema/*.schema.json","reason":null,"links":["qa/manifests/T-001-knowledge-base-schema.md","qa/verdicts/T-001-knowledge-base-schema.md"]}
{"id":"T-004","feature":"tree-index-generator","event":"shipped","date":"2026-09-03","unit":"tree_index/build_tree.py","reason":null,"links":["qa/manifests/T-004-tree-index-generator.md","qa/verdicts/T-004-tree-index-generator.md"]}
{"id":"T-005","feature":"ask-router","event":"shipped","date":"2026-09-03","unit":"ask_router/evaluator.py","reason":null,"links":["qa/manifests/T-005-ask-router.md","qa/verdicts/T-005-ask-router.md"]}
{"id":"T-016","feature":"monorepo-restructure","event":"shipped","date":"2026-09-03","unit":"package.json, pnpm-workspace.yaml, packages/*","reason":null,"links":["qa/manifests/T-016-monorepo-restructure.md","qa/verdicts/T-016-monorepo-restructure.md"]}
{"id":"T-017","feature":"structure-lint","event":"shipped","date":"2026-09-03","unit":"scripts/lint-*.mjs","reason":null,"links":["qa/manifests/T-017-structure-lint.md","qa/verdicts/T-017-structure-lint.md"]}
```

## C7 — memory pointer file contents

**`C:\Users\Lenovo\.claude\projects\d--KnowledgeBase\memory\project_snapshot_first.md`** (new):
```
# Read docs/SNAPSHOT.md first for this project

For D:\KnowledgeBase (Living Knowledge Base), read `docs/SNAPSHOT.md` before anything else that
needs current system shape — it is a **generated** read-first digest (system summary from
ARCHITECTURE.md §1, a depth-2 directory tree with per-dir purposes, a schema summary table, the
last 20 `docs/FEATURES.jsonl` rows, and ARCHITECTURE.md §6 open questions), kept under 200 lines
so a session doesn't have to re-read the whole tree to get oriented.

**Never edit `docs/SNAPSHOT.md` directly** — it is overwritten by `node scripts/snapshot.mjs`
and `pnpm lint:structure` fails the build if the committed file is stale (`node
scripts/snapshot.mjs --check`). Edit the sources instead:
- `ARCHITECTURE.md` (§1 system paragraph, §4 directory map, §6 open questions) — via
  `/landplane` / an authorizing `docs/DECISIONS.md` entry, per the Lab Protocol.
- `schema/*.schema.json` — add/change a collection's shape (also regenerates the schema
  summary table).
- `docs/FEATURES.jsonl` — append-only feature lifecycle ledger. Add rows only via
  `appendEvent()` in `scripts/lib/features.mjs` or `node scripts/features-ledger.mjs add
  <json-file>` (never hand-edit the file — that would break append-only-ness and schema
  validation). `reason` is required and non-empty for `updated`/`removed` events.

The session-start hook (`.claude/hooks/mc-sessionstart.ps1`) already surfaces any
`removed`/`updated` `docs/FEATURES.jsonl` row from the last 30 days as a `FEATURE CHANGED:`
line — that is the feature-level anti-cyclic guard, so don't re-propose a recently
removed/updated feature without noticing why it changed.

Contract: `qa/contracts/snapshot-features-ledger.md` (T-017b). Built by: T-017b maker.
```

**`C:\Users\Lenovo\.claude\projects\d--KnowledgeBase\memory\MEMORY.md`** (new):
```
# Memory Index

- [Read docs/SNAPSHOT.md first](project_snapshot_first.md) — generated read-first digest (system
  summary + dir tree + schema table + recent feature events + open questions); never hand-edit
  it, edit ARCHITECTURE.md/schema/docs/FEATURES.jsonl instead; `pnpm lint:structure` fails if it
  goes stale.
```
(Neither file is git-tracked — both live under the user's Claude memory location, outside
D:\KnowledgeBase.)

## Status: ready-for-check
