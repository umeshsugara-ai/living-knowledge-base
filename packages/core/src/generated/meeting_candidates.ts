/* eslint-disable */
/**
 * GENERATED by scripts/gen-types.mjs from schema/meeting_candidates.schema.json — DO NOT EDIT.
 * Run `pnpm gen:types` after changing the schema.
 */

/**
 * A meeting-shaped email found by scanning Gmail (T-028: Gmail meeting-candidate approval workflow). Never auto-ingested as a real session — a human approves it first, or the sending pattern has already earned trust (see trusted_senders). One row per real Gmail message, deduped by messageId.
 */
export interface MeetingCandidates {
  _id: string;
  tenantId: string;
  /**
   * The real Gmail message id — dedup key.
   */
  messageId: string;
  subject: string;
  senderEmail: string;
  senderDomain: string;
  meetingUrl?: string;
  eventDate?: string;
  status: "pending" | "approved" | "rejected" | "auto_approved";
  detectedAt: string;
  decidedAt?: string;
  [k: string]: unknown;
}
