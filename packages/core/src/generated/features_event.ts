/* eslint-disable */
/**
 * GENERATED by scripts/gen-types.mjs from schema/features_event.schema.json — DO NOT EDIT.
 * Run `pnpm gen:types` after changing the schema.
 */

/**
 * One line of docs/FEATURES.jsonl — a single feature lifecycle event (T-017b). The ledger file is append-only newline-delimited JSON; each line independently validates against this schema (matches schema/validate.py's existing per-collection single-doc fixture pattern — no JSONL-aware extension needed).
 */
export type FeaturesEvent = {
  id: string;
  feature: string;
  event: "planned" | "shipped" | "updated" | "removed";
  date: string;
  unit: string;
  reason?: string | null;
  links: string[];
  [k: string]: unknown;
};
